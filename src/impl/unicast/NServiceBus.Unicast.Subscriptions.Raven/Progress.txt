+ Working with external server, had to switch to using an MD5 hash for subscription id though as the id we were using was going to be to long for the HTTP API
+ Exliuded  from interanlization in ILMerge as it needs to be public for MEF
- Raven.Database.Config.InMemoryRavenConfiguration:431 has hardcoded string refencences to transcactional storage proivider types which are fully qualified with assembly names, which means we need to actually need to reference that assembly. When we do NSB will try and scan it as well throwing more errors

* Enable the http server on the embedded server
* Allow configureable enpoint addresses for raven